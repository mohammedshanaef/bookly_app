//When not to use static:
//If you're defining constants globally, there's no need for static because const itself creates a compile-time constant.

import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xff100B20);
const kTrasinationDuration = Duration(seconds: 2);
const kGtSectraFine = 'GT Sectra Fine';
